# Signal Platform dependencies

# Settings
pydantic-settings>=2.0.0

# Scheduling
APScheduler>=3.10.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Chart generation
matplotlib>=3.7.0
mplfinance>=0.12.10b0
pandas>=2.0.0

# Data source: cTrader Open API
ctrader_open_api==0.9.2
httpx>=0.27.0

# Notifications (optional)
python-telegram-bot>=20.0

# TLS hostname verification for Twisted (used by ctrader_open_api)
service_identity>=21.1.0

# AI validation — google-generativeai intentionally omitted here.
# It conflicts with ctrader_open_api over protobuf (3.20.1 vs 4.x).
# The import is lazy inside _call_gemini_sync so the platform boots fine
# without it; Gemini validation is simply skipped when the package is absent.
