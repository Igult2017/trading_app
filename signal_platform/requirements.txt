# Signal Platform dependencies

# Settings
pydantic-settings>=2.0.0

# Scheduling
APScheduler>=3.10.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Chart generation
matplotlib>=3.7.0
mplfinance>=0.12.10b0
pandas>=2.0.0

# Data source 1: cTrader Open API (when Spotware approves your app)
ctrader_open_api
httpx>=0.27.0

# Data source 2 (disabled): MT5 via Wine/Docker — re-enable by uncommenting:
# mt5linux

# Data source 2 (active fallback): ejtraderCT FIX — live price overlay on yfinance bars
ejtraderCT

# Notifications (optional)
python-telegram-bot>=20.0

# AI validation (optional)
google-generativeai>=0.8.0
