# Pinned to 0.9.2 — same as the signal platform + base image. There is no 2.x on
# PyPI (max is 0.9.3); the engine code targets the 0.9.x API. Already satisfied in
# the base image, so this install is a no-op at build time.
ctrader-open-api==0.9.2
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
cryptography>=41.0.0
twisted>=23.0.0
