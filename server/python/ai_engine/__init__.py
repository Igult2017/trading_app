"""
ai_engine — Trading intelligence backend.

Modes:
  analysis  — builds win/loss profiles and surfaces patterns
  qa        — answers specific questions grounded in trade data
  strategy  — derives a data-backed strategy from proven edges

Entry point: main.py (spawned by Node.js, communicates via stdin/stdout)
"""
