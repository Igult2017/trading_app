node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files (never commit secrets)
.env
.env.local
.env.production
__pycache__/
*.pyc

# Diagnostic / scratch files (may contain API responses with secrets — never commit)
apps.json
deploy_status.json
deploy_status_now.json
logs_raw.json
clogs.json
.playwright-mcp/
docs/reference/SD.pdf

# Throwaway scratch dirs — a deploy/query helper must never become a file
# nobody reviews. His rule: 'if a file is not needed anymore it is deleted.'
.tmpdeploy/
scratch/
